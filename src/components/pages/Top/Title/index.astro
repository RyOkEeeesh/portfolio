---
import type { RouteType } from '@/types';

import LinkIcon from '@/components/parts/link/Link/index.astro';
import Title from '@/components/parts/Title/index.astro';

interface Props {
  route: RouteType;
}

const { route } = Astro.props;
---

<div class="title-content">
  <Title>{route}</Title>
  <LinkIcon href={route}><span class="link-text">view all</span></LinkIcon>
</div>

<style>
  .title-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    user-select: none;
    border-bottom: 2px solid var(--clr-border);
    padding: 0.5rem 0.25rem;
  }

  .link-text {
    opacity: calc(0.6 + var(--is-hovered, 0) * 0.4);
    transition: opacity var(--duration-common);
  }
</style>
